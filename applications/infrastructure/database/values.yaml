postgresql:
  auth:
    database: minikafka

  primary:
    persistence:
      enabled: true
      size: 8Gi


flyway:
  image:
    repository: ophkmh/kafka-mini-project-database
    tag: "v1.0"

  database:
    url: jdbc:postgresql://postgres-postgresql:5432/minikafka
    secretName: postgres-postgresql