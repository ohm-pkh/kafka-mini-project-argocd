apiVersion: v2
name: longhorn-setup
description: Install longhorn to be storange for pvc
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: longhorn
    version: "1.12.0"   
    repository: "https://charts.longhorn.io"