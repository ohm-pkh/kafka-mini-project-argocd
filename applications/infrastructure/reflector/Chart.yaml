apiVersion: v2
name: reflector-setup
description: Install Reflector and auto-annotate secrets for cross-namespace sync
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: reflector
    version: "10.0.60"   
    repository: "https://emberstack.github.io/helm-charts"